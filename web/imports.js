import { post, esc, enc, page, submit, action, feedback } from "./ui.js";
import { botModelPicker } from "./model-picker.js";

export async function renderImports(ctx) {
  const sources = await ctx.get("/api/imports/sources"); if (!ctx.current()) return;
  ctx.root.innerHTML = page("Bring your bots over", "Choose several bots at once, review what comes across, then bring them into Linubot.", `<section class="section"><h2>Bring memory and skills too</h2><p>Local Hermes profiles include their saved knowledge. For Grok cloud data, save the bot’s actual memory and skill files in an exported folder, then add it here. Cache-only imports cannot include cloud-only files.</p><div class="actions"><select aria-label="Exported folder source" data-folder-source><option value="grok">Grok exported folder</option><option value="hermes">Hermes exported folder</option></select><button type="button" class="small" data-add-folder>Choose exported folder</button></div><details><summary>What should the folder contain?</summary><p>Use SOUL.md for your bot’s instructions; MEMORY.md, USER.md or memories/ for saved knowledge; and skills/NAME/SKILL.md with supporting files. Download or unpack the folder on Linux first. This is a file-transfer format, not an automatic Grok cloud export. Credentials and browser logins are excluded.</p></details><div data-feedback hidden></div></section><form class="settings-form" data-import-form><fieldset data-sources disabled><legend>Choose bots and groups</legend><div class="actions"><button type="button" class="small" data-select-all>Select all</button><button type="button" class="small" data-clear-selection>Clear selection</button><span class="field-hint" data-selected-count role="status">0 selected</span></div>${["hermes", "grok"].map((kind) => `<fieldset><legend>${kind === "hermes" ? "Hermes profiles" : "Grok Bot"}</legend>${sources.candidates.filter((item) => item.source === kind).map((item) => `<label class="check-label"><input type="checkbox" name="sourceId" value="${esc(item.id)}">${esc(item.name)}${item.kind === "group" ? " (group)" : ""}${item.importedAs ? ` · imported as ${esc(item.importedAs)}` : ""}</label>`).join("")}</fieldset>`).join("")}</fieldset><p class="field-hint" data-source-detail></p><label data-target-name>Linubot name<input name="name" maxlength="40" pattern="[A-Za-z0-9][A-Za-z0-9_-]{0,39}" placeholder="Choose a name"></label><div data-import-model></div><fieldset><legend>Bring along</legend><label class="check-label"><input type="checkbox" name="memory" checked>Saved memories, kept with this bot</label><label class="check-label"><input type="checkbox" name="skills" checked>Skills and supporting files</label><label class="check-label"><input type="checkbox" name="activateSkills" checked>Attach imported skills so the bot can use them</label><label class="check-label"><input type="checkbox" name="history" checked>Recent conversation history</label><label class="check-label"><input type="checkbox" name="routines">Compatible routines, paused until you enable them</label></fieldset><div data-feedback hidden></div><div class="form-actions"><button class="primary" type="submit">Preview import</button></div></form><section class="section" data-import-preview hidden></section><details class="section"><summary>Source locations and supported data</summary><p>Hermes: <code>${esc(sources.locations.hermes)}</code></p><p>Grok Bot cache: <code>${esc(sources.locations.grok)}</code></p><p>Hermes history includes up to three recent conversations. Grok Bot supplies locally cached descriptions, groups and recent messages. Its cloud-only instructions, memories, workspace files, skills and schedules are not in this cache.</p><p>Source apps keep their data. Account credentials, approvals, active tasks and automatic posting are not transferred.</p>${sources.warnings.map((warning) => `<p>${esc(warning)}</p>`).join("")}</details>`, '<a class="button small" href="#/home">Back to your bots</a>', "Your bots / Import");
  const form = ctx.root.querySelector("[data-import-form]"), review = ctx.root.querySelector("[data-import-preview]");
  ctx.root.querySelector('[data-add-folder]').onclick = () => void action(ctx.root.querySelector('[data-add-folder]').closest('section'), async () => {
    const result = await post('/api/imports/folder', {source:ctx.root.querySelector('[data-folder-source]').value}, {timeout:300000});
    if (ctx.current() && !result.cancelled) ctx.reload();
  }, 'Choose the exported folder on Linux…');
  form.querySelector("button[type=submit]").disabled = true;
  const choices = [...form.querySelectorAll("[name=sourceId]")];
  const selection = () => sources.candidates.filter(item => choices.some(choice => choice.checked && choice.value === item.id));
  let revision = 0, prepared;
  const picker = await botModelPicker(form.querySelector("[data-import-model]"), {}, ctx.signal); if (!ctx.current()) return;
  function invalidate() { revision++; prepared = undefined; review.hidden = true; }
  form.addEventListener("input", invalidate); form.addEventListener("change", invalidate);
  function selectionChanged() {
    const selected = selection(), source = selected.length === 1 ? selected[0] : undefined;
    form.elements.name.value = source?.name.normalize("NFKD").replace(/[^A-Za-z0-9_-]+/g, "-").replace(/^[-_]+|[-_]+$/g, "").slice(0, 32) || (source?.source === "hermes" ? "Hermes" : "Grok");
    form.querySelector("[data-target-name]").hidden = source?.kind !== "bot";
    form.elements.name.disabled = source?.kind !== "bot";
    form.querySelector("[data-selected-count]").textContent = `${selected.length} selected`;
    form.querySelector("[data-source-detail]").textContent = selected.length > 1 ? "Each bot keeps its name. Group members and already imported bots are reused, without duplicates." : source?.kind === "group" ? "The group and its members come over together. Already imported members are reused." : source?.description || "";
    invalidate();
  }
  choices.forEach(choice => { choice.onchange = selectionChanged; });
  form.querySelector("[data-select-all]").onclick = () => { choices.forEach(choice => { choice.checked = true; }); selectionChanged(); };
  form.querySelector("[data-clear-selection]").onclick = () => { choices.forEach(choice => { choice.checked = false; }); selectionChanged(); };
  submit(form, async () => {
    const selected = selection(), source = selected.length === 1 ? selected[0] : undefined; if (!selected.length) throw new Error("Choose one or more bots or groups to import.");
    const current = revision;
    const value = await post("/api/imports/preview", { sourceIds: selected.map(item => item.id), name: source?.kind === "bot" ? form.elements.name.value : undefined, ...picker.value(), memory: form.elements.memory.checked, skills: form.elements.skills.checked, history: form.elements.history.checked, routines: form.elements.routines.checked, activateSkills: form.elements.activateSkills.checked }, { timeout: 60000 });
    if (!ctx.current() || current !== revision) return;
    prepared = value; review.hidden = false; feedback(form, "Preview ready. Review the snapshot below.", "success");
    review.innerHTML = `<h2>Review the import</h2><p>${value.groups?.length ? `Groups: ${value.groups.map(group => esc(group.name)).join(", ")}. ` : ""}New bots will use ${esc(value.provider.name)} · ${esc(value.provider.model)}.</p>${value.bots.map((bot, index) => `<article class="import-preview-bot"><h3>${esc(bot.name)}${value.targets[index].existing ? " · already imported" : ""}</h3><p>${esc(bot.description)}</p><p class="field-hint">${bot.memoryBytes || 0} bytes of saved memory · ${bot.messages} historical messages · ${bot.skills.length} ${value.activateSkills ? "attached" : "draft"} skills · ${bot.routines.length} paused routines${bot.sourceModel ? ` · source model: ${esc(bot.sourceModel)}` : ""}</p>${bot.soul ? `<details><summary>Personality and instructions</summary><pre class="technical">${esc(bot.soul)}</pre></details>` : ""}${bot.context ? `<details><summary>Saved context for this bot</summary><pre class="technical">${esc(bot.context)}</pre></details>` : ""}${bot.skills.length ? `<details><summary>Imported skills and supporting files</summary><ul>${bot.skills.map((skill) => `<li>${esc(skill.name)}: ${esc(skill.description)} (${skill.files} supporting files)</li>`).join("")}</ul></details>` : ""}</article>`).join("")}${value.warnings.length ? `<details open><summary>Items to check</summary><ul>${value.warnings.map((warning) => `<li>${esc(warning)}</li>`).join("")}</ul></details>` : ""}<div data-feedback hidden></div><div class="form-actions"><button type="button" class="primary" data-confirm-import>Import into Linubot</button></div>`;
    review.querySelector("[data-confirm-import]").onclick = () => void action(review, async () => {
      if (prepared?.id !== value.id) throw new Error("Preview the current choices before importing.");
      const result = await post("/api/imports/commit", { id: value.id }, { timeout: 60000 }); if (!ctx.current()) return;
      prepared = undefined; ctx.changed();
      const [kind, name] = result.scope.split(":");
      const bulk = selected.length > 1;
      review.innerHTML = `<h2>Your teammates are ready</h2><p>${result.bots.map(esc).join(", ")} ${result.bots.length === 1 ? "is" : "are"} now in Linubot.</p>${result.skills.length ? `<p>${result.skills.length} imported skills ${value.activateSkills ? "are attached and available to their bots." : "are drafts in the Library; approve and attach them when ready."}</p>` : ""}${result.routines.length ? `<p>${result.routines.length} routines are paused in Routines.</p>` : ""}<div class="actions">${bulk ? '<a class="button primary" href="#/home">Open your bots</a>' : `<a class="button primary" href="#/${kind}/${enc(name)}">Open ${kind === "group" ? "group" : "bot"}</a>`}${result.skills.length ? '<a class="button" href="#/library">Review skills</a>' : ""}</div>`;
    }, "Importing the reviewed snapshot…");
    review.scrollIntoView({ behavior: "smooth", block: "start" });
  }, "Preparing an import preview…");
  if (!sources.candidates.length) feedback(form, "No supported Hermes profiles or Grok Bot caches were found at the locations below.", "info");
  form.querySelector("[data-sources]").disabled = false; form.querySelector("button[type=submit]").disabled = !choices.length;
  selectionChanged();
}
